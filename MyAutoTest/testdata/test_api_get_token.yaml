- name: ${name}
  request:
    url: https://api.weixin.qq.com/cgi-bin/token
    method: POST
    data:
      grant_type: client_credential
      appid: ${appid}
      secret: ${secret}
    validate: ${validate}
